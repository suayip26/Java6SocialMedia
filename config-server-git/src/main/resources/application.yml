server:
  port: 8889

spring:
  application:
    name: config-server-git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/suayip26/java6ConfigServer2.git
          default-label: main
          skip-ssl-validation: true
          username: suayip26
          password: ${java6configpassword}